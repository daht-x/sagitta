#!/usr/bin/env sh

dotnet clean
dotnet restore
readonly build_configuration='Release'
dotnet build \
	--no-restore \
	--configuration "$build_configuration"
dotnet test \
	--no-build \
	--configuration "$build_configuration"
